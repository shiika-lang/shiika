class Main
  def self.run
    var a = 1
    var b = 2
    let g = fn(x: Int){ x }
    let f = fn(x: Int){ a += x; b += g(x) }
    f(10)
    let result = a + b
    unless result == 23
      puts "ng: lambda"
    end
  end
end
Main.run
puts "ok"
